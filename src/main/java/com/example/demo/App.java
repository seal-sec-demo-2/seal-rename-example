package com.example.demo;

/**
 * Minimal placeholder entry point.
 *
 * This repository exists to demonstrate how dependency coordinates appear in a
 * pom.xml after sealed-name renaming, so SCA tools can scan the renamed packages.
 * The application logic itself is not the focus.
 */
public class App {
    public static void main(String[] args) {
        System.out.println("seal-rename-example");
    }
}
