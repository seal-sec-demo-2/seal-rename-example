# seal-rename-example

An example Maven project showing how dependency coordinates appear in a `pom.xml`
once sealed (backported) builds have been applied using sealed-name renaming.
Intended as a sample for SCA tooling (e.g. Cycode) to scan.
